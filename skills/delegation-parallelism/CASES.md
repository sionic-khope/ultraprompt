# delegation-parallelism — Trace evidence

Case runs that exercised the delegation-parallelism axis. One row per real Claude Fable 5.1 session (per `_SIMULATION.md`).
Rows are recorded from real traces only — never invented. Until the first row lands, this skill stays a
`v0.1 baseline draft`; the first real row promotes it.

Provenance: draft axis, co-developed in maestro-ultra (authoring working copy); mirrored here to rlaope/ultraprompt (publishing home).

| Case | Session | Task / domain | Distinctness shown vs existing axes | What it confirmed / contradicted / refined |
|------|---------|---------------|-------------------------------------|--------------------------------------------|
| _awaiting case runs_ | — | — | — | — |
